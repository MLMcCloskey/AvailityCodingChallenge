/// Thoughts for how to lay out the project.....

/*
Write an app in your programming language of choice that takes as input a list of files and a provider parameter.
 Depending on the provider parameter either write the files to a database, the file system, or print the 
 files to console or popup window. 
 The parameter values are “provider1” for database, “provider2” for file system, “provider3” for console/popup.


create upload form for files. use a radio to give it 3 options, p1 p2 p3

conditionally upload the files based on the parameter fromt the radio

if parameter = provider1  {
    use mongo
}

if parameter = provider2 {
    use fs
}

if parameter = provider3 {
    console.log? alert?
}


//////////// psuedocode

function upload(file, parameter) {
    if parameter
}


*/